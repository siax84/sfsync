<%= form_for(@test_entry) do |f| %>
  <% if @test_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_entry.errors.count, "error") %> prohibited this test_entry from being saved:</h2>

      <ul>
      <% @test_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
